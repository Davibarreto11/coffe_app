import React from "react";
import { StyleSheet, Text } from "react-native";

const OrderHistory: React.FC = () => {
  return <Text>Order History</Text>
}

const styles = StyleSheet.create({});

export default OrderHistory;
